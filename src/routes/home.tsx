import { Profile } from "@/components/profile";
import { SearchWeather } from "@/components/search-weather";

export const Home = () => {
  return (
    <div>
      <Profile />
      <SearchWeather />
    </div>
  );
};
